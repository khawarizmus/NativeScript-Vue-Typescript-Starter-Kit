// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.starter"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
